import { StyleSheet } from "react-native";

import {
  COLORS,
  SPACING,
} from "../../design";

export default StyleSheet.create({
  title: {
    fontSize: 22,

    fontWeight: "700",

    color: COLORS.text,

    marginBottom: SPACING.md,
  },
});